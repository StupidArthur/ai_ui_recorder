【历史上下文参考】
以下是发生在本次批处理之前的最近几次动作解析结果，仅供你理解上下文逻辑，**不需要**在你的输出中包含它们：
{{contextHistory}}

【本次需要解析的动作数组】
注意：以下共有 {{actionCount}} 个动作。你必须输出 {{actionCount}} 个解析结果。

{{actionBlocks}}

请立即开始解析，并严格按照 Schema 输出 JSON 对象。
